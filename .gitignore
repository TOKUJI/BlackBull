# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Intermediate files
*.lock
*.log

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
*.code-workspace

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/

# vscode
.vscode/

# Private development assets — stored in companion private repo.
# Clone: git clone git@github.com:TOKUJI/blackbull-assets.git ../blackbull-assets
# Symlink: ln -s ../blackbull-assets/.claude .claude
.claude
# CLAUDE.md is a symlink to AGENTS.md — create locally: ln -s AGENTS.md CLAUDE.md
CLAUDE.md

# benchmark result files (keep directory + READMEs, ignore data)
bench/results/*
!bench/results/aws/
!bench/results/baselines/
bench/results/aws/*
bench/results/baselines/*
!bench/results/aws/README.md
!bench/results/baselines/pre_sprint9d/
bench/results/baselines/pre_sprint9d/*
!bench/results/baselines/pre_sprint9d/README.md
# bench/aws — runtime state and SSH private keys created by up.sh
bench/aws/.state
bench/aws/.known_hosts
bench/aws/*.pem
# bench/httparena — local sandbox mounts (dataset + dev certs).  See
# bench/httparena/README.md for the local smoke-test recipe.
bench/httparena/_local/
# A locally-built wheel that Dockerfile.dev installs from.  Untracked
# so the dev image always builds against fresh source.
bench/httparena/blackbull-*.whl
# peer benchmark harness — work-in-progress automated runner.
# Kept on disk for future development; not committed until the socket-state
# contamination is solved cleanly.
bench/peers/compare.sh
bench/peers/httparena_calibrate.py
bench/peers/bench/results/*
# peer benchmark harness — private development notes
*.private.md



# RFC conformance test output (kept directory, ignore data files)
bench/conformance/results/*

# Http11Probe run output (kept directory, ignore data files)
bench/http11probe/results/*.json